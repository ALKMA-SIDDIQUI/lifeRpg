# LIFE RPG — Root Environment Template

# Backend API Configuration
PORT=5000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Database (PostgreSQL)
# For local cluster (default port 5433 or 5432) or cloud provider (Neon, Supabase, Railway)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/liferpg
DATABASE_SSL=false

# Authentication Secret
JWT_SECRET=life_rpg_super_secret_cyber_jwt_key_2026_dev
